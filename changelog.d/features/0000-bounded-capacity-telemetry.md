- **feat(health):** add safe, bounded provider-level capacity telemetry to authenticated `GET /api/monitoring/health`, including queue/in-flight peaks, capacity rejections, upstream 429/502/503 counts, availability/quota state, freshness/truncation metadata, and cached process/database metrics. Public liveness remains `/api/health/ping`; account labels are omitted by default or stably HMAC-masked with `OMNIROUTE_TELEMETRY_HMAC_SECRET`. Counters are process-local and do not claim model-level or multi-replica aggregation.
